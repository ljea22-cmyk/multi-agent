```markdown
# 11주차 실습: 일정공지 에이전트 단계별 가이드

아래 항목을 순서대로 실습하세요. 각 단계은 작동 확인(간단 실행)과 검토 포인트를 포함합니다.

1) 입력 준비

- [ ] `sample_notices.txt`에 공지 3개 이상 준비하기(제목/대상/기간/준비물/내용 포함).

검토 포인트: 파일을 열어 공지 블록이 빈 줄로 구분되어 있는지 확인.

2) 사실 추출(Fact extraction)

- [ ] `extract_fields(text)` 함수 작성: 공지에서 제목, 대상, 기간, 준비물, 본문을 추출.
- [ ] 추출 결과를 한 건씩 콘솔에 출력해 확인.

검토 포인트: 각 공지에서 최소한 제목/대상이 잘 추출되는지 확인. 실패 케이스는 로그에 표시.

3) 기간(날짜) 정규화

- [ ] `normalize_period(raw_period)` 작성: YYYY-MM-DD 또는 범위 형태를 표준화.
- [ ] 모호한 표현은 검토 목록에 남기기.

검토 포인트: 단일일/범위가 ISO 형식으로 변환되는지 확인.

4) 대상 분류(Classification)

- [ ] `classify_targets(target_text)` 작성: 'all_students', 'freshmen', 'graduate_students', 'professors' 등으로 라벨링.

검토 포인트: 한 공지가 여러 대상이면 각 대상 그룹에 포함되는지 확인.

5) 안내문 생성

- [ ] 템플릿 기반으로 사용자별 안내문(짧은 한두 문장) 생성 함수 작성.

검토 포인트: 대상별로 읽기 쉬운 문장으로 출력되는지 확인.

6) 전체 파이프라인 연결

- [ ] `main()`에서 파일 로드 → 분할 → 추출 → 정규화 → 분류 → 렌더(요약표 + 사용자 안내) 순으로 실행.
- [ ] `output.md`, `output_user_guide.md`, `review_report.md` 생성 확인.

검토 포인트: 전체 실행 시 오류 없이 파일들이 생성되는지 확인하고, `review_report.md`에 예외/경고가 기록되는지 확인.

7) 간단한 테스트 및 리팩터

- [ ] `tests/test_extract.py` 같은 간단한 유닛 테스트 1~2개 추가(추출/정규화 핵심 케이스).
- [ ] 코드 정리: 함수 분리, 주석, 에러 메시지 정리.

검토 포인트: 테스트 통과 및 코드 가독성 확보.

8) (선택) 고급 개선

- [ ] 자연어 날짜 파싱 라이브러리 도입 검토(`dateparser` 등).
- [ ] 불확실 추출에 대한 수동 검토 인터페이스(간단한 로그/태그) 추가.
```markdown
# 실습 일정 (11주차 ~ 14주차)

11주차 — 뼈대 만들기

- [ ] `schedule_agent.py`에 입력 파일 로드, 공지 분할, 간단한 출력(write)을 구현한다.
- [ ] `sample_notices.txt` 예시 파일 준비(3건 이상).

검토 포인트: `python schedule_agent.py` 실행 시 최소 `output.md`가 생성되는지 확인.

12주차 — 사실 추출

- [ ] `extract_fields(text)` 구현: 제목, 대상, 기간, 준비물, 본문 추출.
- [ ] `normalize_period(raw)` 구현: YYYY-MM-DD 형식 또는 범위를 반환.
- [ ] 추출 실패 항목을 `review_report.md`에 기록.

검토 포인트: 각 공지에서 제목/대상/기간이 올바르게 추출되는지 샘플로 확인.

13주차 — 사용자별 안내문

- [ ] `classify_targets(text)`로 사용자 그룹 라벨링 구현.
- [ ] 템플릿으로 그룹별 안내문을 생성해 `output_user_guide.md`로 저장.

검토 포인트: 각 그룹에 대해 적어도 한 건의 안내문이 생성되는지 확인.

14주차 — 최종 정리

- [ ] 코드 정리 및 주석 추가, `README.md`에 실행 방법 기술.
- [ ] 간단한 유닛 테스트(추출/정규화) 추가 및 실행.
- [ ] 개선 아이디어(날짜 파싱 라이브러리 도입 등)를 정리해 문서화.

검토 포인트: 전체 파이프라인이 오류 없이 동작하고, 산출물이 문서화되어 있는지 확인.

```