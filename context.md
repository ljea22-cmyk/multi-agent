```markdown
# 일정공지 에이전트 — 프로젝트 컨텍스트(초안)

이 문서는 초보자가 바로 이해하고 실습할 수 있도록 입력, 출력, 역할, 그리고 하지 않을 일을 간단히 정리한 초안입니다.

## 목표

여러 학과에서 온 공지(텍스트)를 읽어서 공지별 핵심 일정(날짜·대상·준비물)을 표로 정리하고, 사용자(학생/교수/학년)별로 간단한 안내문을 자동 생성하는 것입니다.

## 입력

- `sample_notices.txt` 같은 텍스트 파일: 여러 공지가 빈 줄로 구분되어 들어있음.
- 각 공지에는 보통 제목, 대상(예: 1학년, 모든 학생), 기간(예: 2026-05-10), 준비물, 상세 내용이 포함됩니다.

## 출력

- `output.md`: 공지별 요약 표(제목 | 대상 | 기간 | 준비물).
- `output_user_guide.md`: 사용자 그룹별 안내문(한두 문장씩).
- `review_report.md`: 자동 추출에서 실패하거나 모호한 항목 목록(검토용).

## 역할(3개)

1) 사실 추출 에이전트 (Fact extractor)
- 입력: 공지 텍스트 한 건
- 작업: 제목, 대상, 기간, 준비물, 본문 요약을 규칙(정규식) 기반으로 뽑아 냄
- 출력: 구조화된 사실 객체(예: {title, targets, period, items, content})
- 검증 기준: 제목과 대상이 제대로 추출되는지(샘플로 눈검사)

2) 기간/분류 에이전트 (Normalizer & Classifier)
- 입력: 사실 객체
- 작업: 기간(날짜)을 표준 형식(YYYY-MM-DD 또는 범위)으로 정규화하고, 대상 텍스트를 간단한 그룹 라벨(예: all_students, freshmen, professors)로 매핑
- 출력: 정규화된 사실(예: period_norm)과 대상 그룹 리스트
- 검증 기준: 날짜가 ISO 형태로 바뀌는지, 그룹이 사람이 이해할 수 있는 라벨인지 확인

3) 안내문 작성 에이전트 (User guide renderer)
- 입력: 정규화된 사실들과 대상 그룹
- 작업: 템플릿 기반으로 각 그룹에 보낼 짧은 안내문(1~2문장)을 생성
- 출력: `output_user_guide.md`에 그룹별 안내문 저장
- 검증 기준: 안내문이 읽기 쉽고 준비물/일정 정보가 포함되어 있는지 확인

## 처리 흐름(간단)

sample_notices.txt → 공지 분할 → 사실 추출 → 기간 정규화/대상 분류 → 그룹별 안내문 생성 → 출력 파일 생성 → 검토(report)

## 하지 않을 일(범위 제외 항목)

- 자연어 이해에 지나치게 의존한 복잡한 추론(예: 문맥 기반 심층 의도 해석)은 하지 않습니다.
- 외부 대규모 언어 모델 API를 연동해 고급 생성(예: RAG, LangGraph)하는 작업은 이번 실습 범위에서 제외합니다.
- 실시간 알림/배포(이메일·메시지 전송) 자동화는 포함하지 않습니다 — 출력 파일을 사람이 검토한 뒤 수동 배포합니다.

```